# @modularcore/modals has no required environment variables.
# It is a client-side, provider-agnostic package (no DB, no built-in backend).
# If you connect a real backend via a custom ModalsProvider, its credentials
# live in YOUR backend/env, never in this package.
